import 'package:flutter/material.dart';

/// 书法对比页面 - 占位页面
class CalligraphyCompareScreen extends StatelessWidget {
  const CalligraphyCompareScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('书法对比')),
      body: const Center(child: Text('书法对比')),
    );
  }
}
